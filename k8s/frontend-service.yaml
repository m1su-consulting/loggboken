apiVersion: v1
kind: Service
metadata:
  name: loggboken-frontend
  namespace: loggboken
  labels:
    app.kubernetes.io/name: loggboken
    app.kubernetes.io/component: frontend
spec:
  selector:
    app.kubernetes.io/name: loggboken
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
